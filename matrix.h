#ifndef MATRIX_H
#define MATRIX_H

#include<iostream>
using size_type = std::size_t;

template<typename T, size_type Rows, size_type Columns>
class Matrix{
    public:

    private:
        Array<Array<Type,Rows>,Columns>
};


#endif